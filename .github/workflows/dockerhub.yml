name: Build and Push Docker Image
# name: CI/CD Pipeline - Satu Atap by BNI

on:
  push:
    branches: [master]
    tags: ["v*"]
  pull_request:
    branches: [master]

jobs:
  docker:
  # build-test-scan:
  #   name: Build, Test, and Scan
    runs-on: ubuntu-latest

    steps:
      # [Step 1] Checkout Repository
      # - name: Checkout Repository
      - name: Checkout
        uses: actions/checkout@v4

      # # [Step 2] Setup JDK
      # - name: Set up JDK 21
      #   uses: actions/setup-java@v4
      #   with:
      #     java-version: '21'
      #     distribution: 'temurin'
      #     cache: 'maven'

      # # [Step 3] Build Project
      # - name: Build Project
      #   run: mvn clean package -DskipTests=false

      # # [Step 4] Run Unit Tests
      # - name: Run Unit Tests
      #   run: mvn test

      # # [Step 5] SonarCloud Code Quality Scan
      # - name: SonarCloud Scan
      #   uses: SonarSource/sonarcloud-github-action@v3
      #   env:
      #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      #   with:
      #     args: >
      #       -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
      #       -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
      #       -Dsonar.host.url=https://sonarcloud.io
      #       -Dsonar.java.binaries=target/classes

      # # [Step 6] Enforce Quality Gate
      # - name: Wait for SonarCloud Quality Gate
      #   uses: SonarSource/sonarcloud-quality-gate-action@v1
      #   env:
      #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      # # [Step 7] OWASP Dependency Check
      # - name: Run OWASP Dependency Check
      #   run: |
      #     mvn org.owasp:dependency-check-maven:check
      #     cat target/dependency-check-report.html || true
      #   continue-on-error: false

      # # [Step 8] Security Scan with Trivy
      # - name: Run Trivy Scan
      #   uses: aquasecurity/trivy-action@master
      #   with:
      #     image-ref: ${{ secrets.DOCKERHUB_USERNAME }}/griya-kpr:latest
      #     format: 'table'
      #     exit-code: '0'
      #     vuln-type: 'os,library'
      #     severity: 'CRITICAL,HIGH'
      
      # [Step 9] Setup Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # [Step 10] Docker Login
      - name: Log in to Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # [Step 11] Extract Docker Metadata
      - name: Extract Docker metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/griya-kpr
          tags: |
            type=ref,event=tag
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=short

      # [Step 12] Build and Push Docker Image
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64
    
